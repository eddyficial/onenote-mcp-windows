{
  "name": "onenote-mcp-windows",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "description": "Standalone MCP server for Microsoft OneNote desktop on Windows",
  "scripts": {
    "start": "bun run src/mcp-server.ts",
    "test": "bun test",
    "check": "bunx tsc --noEmit",
    "setup": "bun run scripts/setup.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "packageManager": "bun@1.3.14"
}
